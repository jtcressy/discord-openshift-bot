from setuptools import setup

setup(
        name='openshiftbot',
        packages=['openshiftbot'],
        include_package_data=True,
        install_requires=[
            'discord.py'
        ],
)
